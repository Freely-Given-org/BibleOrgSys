[package]
name = "usfm_markers_py"
version = "0.3.0"
edition.workspace = true

[lib]
name = "usfm_markers_py"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { workspace = true, features = ["extension-module", "abi3-py312"] }
usfm_markers = { path = "../usfm_markers" }  # Local dependency
compact_str.workspace = true
